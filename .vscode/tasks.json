{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build simKeyTrans",
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "windres resource.rc -O coff -o resource.res && g++ main.cpp resource.res -O0 -g -mwindows -static -static-libgcc -static-libstdc++ -lurlmon -o simKeyTrans.exe"
            ],
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}